apiVersion: v1
kind: Service
metadata:
  name: {{ include "foyre.fullname" . }}
  labels: {{- include "foyre.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector: {{- include "foyre.selectorLabels" . | nindent 4 }}
